setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
    mjml: mjml/Dockerfile
  config:
release:
  image: web
  command:
    - python manage.py collectstatic --noinput
run:
  web: gunicorn core.wsgi
